# Copy to strapi-secrets.env (chmod 600). Loaded by docker-compose.yml via env_file.
# Changing API_TOKEN_SALT invalidates every Strapi API token.
APP_KEYS=
API_TOKEN_SALT=
ADMIN_JWT_SECRET=
TRANSFER_TOKEN_SALT=
JWT_SECRET=
ENCRYPTION_KEY=
